import ExpenseTrackerContainer from "@/feat/expense-tracker";

const ExpenseTracker = () => {
  return (
    <div>
      <ExpenseTrackerContainer />
    </div>
  );
};

export default ExpenseTracker;
